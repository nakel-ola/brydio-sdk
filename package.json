{
  "name": "brydio-sdk",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "templates/*"
  ],
  "scripts": {
    "test": "bun test",
    "check-types": "tsc -p tsconfig.json && tsc -p templates/preact/tsconfig.json && tsc -p templates/plain/tsconfig.json",
    "contracts:record": "BRYDIO_RECORD=1 bun test"
  },
  "devDependencies": {
    "@types/bun": "1.4.2",
    "typescript": "5.9.3"
  },
  "devEngines": {
    "packageManager": {
      "name": "bun",
      "version": "1.3.11"
    }
  }
}
